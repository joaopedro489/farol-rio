const API_BASE_ROUTE = '/api/dashboard'

export const DashboardSettingsRoutes = {
  SUMMARY: `${API_BASE_ROUTE}/summary`,
  ALERTS_BY_TYPE: `${API_BASE_ROUTE}/alerts-by-type`,
  ALERTS_BY_SEVERITY: `${API_BASE_ROUTE}/alerts-by-severity`,
  ALERTS_BY_NEIGHBORHOOD: `${API_BASE_ROUTE}/alerts-by-neighborhood`,
}
